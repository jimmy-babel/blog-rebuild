# 微信文章原创重构项目规则

## 固定目录

- 角色参考图只从 `photos/` 读取。当前约定文件为 `喜1.png`、`怒1.png`、`哀1.png`、`乐1.png`、`通用1.png`。
- 成品只写入 `result/`。临时抓取文件写入 `.recreate-work/`，不得混入成品目录。
- 项目级 skill 位于 `.agents/skills/recreate-wechat-article/`。
- n8n 提取工作流为 `wechat-article-extractor`；不得修改现有 `test-2`。

## 原创与清洗

- 只保留来源文章的事实、主题和可验证观点。必须重写标题、结构、论证顺序、例子、段落组织与表达。
- 禁止逐段近义词替换、句式微调式洗稿或复制原文长句。`rebuild` 图片模式不得复刻原图构图；`copy` 仅可保留原图的动作、关键道具、空间关系和信息功能，不得复制独特画风或像素级布局。
- 重写前先统计清洗后正文的非空白字符数；成稿目标为原文的 50%～70%，且不得超过 800 个非空白字符。短原文不得为了凑字数而扩写。
- 文章模式使用 `--article-mode trans|ori`（缩写 `-a`），默认 `trans`。`trans` 执行压缩和原创重写；`ori` 只做二次语义清洗，正文原文直出，不执行压缩、改写或成稿长度限制。
- 压缩时优先保留核心事实、主要结论和必要因果，删除重复观点、冗余铺垫及非必要案例。
- 删除作者/编辑/来源署名、二维码、关注引导、广告、推广、赞赏、联系方式、往期推荐和无关装饰。
- 最终 HTML 页面不得显示原作者区块；来源 URL 写入 HTML 元数据，并记录在同目录的 `source.md` 中供内部追溯。

## 图片

- 每张保留图片都要依据画面语义、图片文案和邻近正文判定为喜、乐、怒、哀或通用。
- 无法判断时，使用“文章 URL + 图片序号”的哈希，从喜、乐、通用中稳定选择。
- 图片模式使用 `--image-mode rebuild|copy|comic`（缩写 `-m`），默认 `copy`。`rebuild` 只向 imagegen 提交品牌人物参考图并重新设计场景；`copy` 同时提交来源图片和品牌人物参考图，来源图只决定功能性构图，品牌图优先决定人物身份和视觉风格。`comic` 使用 `--comic-count N`（缩写 `-n`，默认 5），只根据清洗后的文字生成连续叙事图，不读取或参考来源图片，并固定搭配 `article-mode ori`。
- 品牌人物参考使用 `--brand on|off`（缩写 `-b`），默认 `on`。关闭时所有模式都不提交 `photos/` 品牌人物图；`copy` 仍提交来源图片。
- 文案模式使用 `--caption-mode ai|local`（缩写 `-c`），默认 `ai`。`ai` 由 imagegen 直接生成包含中文文案的完整纵向卡片；`local` 只生成无字上半部分，再由 `compose_card.py` 排版下半部分。
- 只有 `local` 文案去除空白后最多 20 个可见字符，标点计入；超过限制时必须重新提炼，不得截断。`ai` 不设字符数、分隔线、字体或文字排版限制，但最终画布固定为 1122×1402，并统一使用示例取样的淡灰白背景 `#F7F6F6`。
- `local` 使用情绪浅色铺满下半部分，取消白色留边、内部圆角框和描边，并使用原规格 1.5 倍字号及稳定的逐字旋转和上下错落。`ai` 由 imagegen 直接完成整张卡片：`copy + ai` 跟随原图的整张信息结构与设计气质，`rebuild + ai` 自由重构丰富场景和卡片设计；两者都必须让主体与文案充分利用画布，避免过量上下留白。
- 先准备全部图片任务，再一次性并发提交所有独立 imagegen 请求；不得分批串行等待。`local` 的成功插画也必须一次性并发执行本地合成。每张图片只尝试一次，不做 OCR、错字检测或重试，也不使用本地模板兜底。
- imagegen 失败时在新文章原位置显示可点击的“原始图片链接：HTTPS URL”；没有有效 URL 时显示“原始图片链接：不可用”并告警。禁止把来源图片作为最终配图嵌入 HTML。
- `ai` 的首次成功图先保存到 `raw-cards/`，再由 `normalize_ai_card.py` 并发裁除过量外围留白、统一边缘背景并输出 `cards/` 下的 1122×1402 PNG；归一失败按生图失败处理。`local` 仍保持 1080×1440，不调用该脚本。

## 输出

- 每篇文章输出到 `result/MMDD-新标题/`；目录内包含同名 HTML、`source.md`、新图 `images/`，以及存在可用原图时的 `source-images/`。日期使用 `Asia/Shanghai`。
- 新图固定命名为 `images/image-001.png`、`image-002.png`……；原图按原文图片顺序复制为 `source-images/image-001.<原扩展名>`……。HTML 和 Markdown 只使用相对本地图片路径。
- `source.md` 按二次语义清洗后的原始图文顺序保存原文正文；每张原图使用本地相对路径展示，并紧接记录对应 HTTPS 原始图片 URL。
- 原图下载失败时不得中止整篇输出；在 `source.md` 对应位置写明失败并保留原始 URL，同时在交付结果中报告告警。没有可用本地原图时不得创建空的 `source-images/`。
- `sourceCharacterCount` 必须等于 `source.md` 所归档文本块的非空白字符数。
- 文件名中的 Windows 非法字符必须清理。同名文章目录存在时追加 `-HHmmss`，目录内 HTML 使用追加时间后的同名文件，绝不覆盖旧结果。
- 保持图片之间、图片与相关章节的相对顺序，不要求逐段对齐来源文章。
- HTML 的正文、导语和引用按 `，。！？；,.!;?` 的结束位置分行，保留标点并居中显示；标题和章节标题不拆分但同样居中。
- 完整 `manifest.json` 必须由 Skill 的 `build_manifest.py` 根据 `source.json` 和精简 `article-plan.json` 自动生成，不得手工复制原文 blocks 拼装。
- 完成前必须校验 HTML、`source.md` 及全部本地资源无断链，并实际打开或渲染 HTML 做视觉检查。

## Skill 与 Web 隔离

- 在 Codex 中原生调用 `$recreate-wechat-article` 时，只执行本 Skill 的文档、脚本和 Codex 原生图片生成流程。
- Skill 原生流程不得导入或调用 `web/app.py`、`web/website_pipeline.py`、Web HTTP API 或 `web/.env`。
- `web/` 是独立的本地网站 runner；只有启动网站时才读取 `web/.env` 中的第三方 API 配置。
